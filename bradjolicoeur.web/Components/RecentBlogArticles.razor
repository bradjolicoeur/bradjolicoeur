
@if(Articles != null)
{
    foreach(var item in Articles)
    {
        <BlogArticleShortSummary Model="@item" />
    }
}

@code {
    [Parameter]
    public IEnumerable<BlogArticle> Articles { get; set; } = null;
}
