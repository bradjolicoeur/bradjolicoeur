@page
@model BlogModel
@{
    ViewData["Title"] = "Blog";
}

<h2>@Model.ViewModel.ContentPage.Title</h2>

@foreach (var article in Model.ViewModel.BlogArticles)
{
    @Html.DisplayFor(vm => article, "BlogArticleSummary");
}


