@page
@model IndexModel
@{
    ViewData["Title"] = Model.ViewModel.Title;
    ViewBag.Description = Model.ViewModel.Title;
    ViewBag.Image = Model.ViewModel.Image?.FirstOrDefault()?.Url;
}


<div class="container">
    <div class="row">
        @Html.DisplayFor(vm => vm.ViewModel.Body)
    </div>
</div>

