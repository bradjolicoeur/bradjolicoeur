@page
@model IndexModel
@{
    ViewData["Title"] = Model.ViewModel.Title;
}

<h2>@Model.ViewModel.Title</h2>

<div class="container">
    <div class="row">
        @Html.DisplayFor(vm => vm.ViewModel.Body)
    </div>
</div>

